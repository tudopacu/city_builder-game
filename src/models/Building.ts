export interface Building {
    id: number;
    name: string;
    size: number;  // Size of the building (e.g., 1x1, 2x2)
    x: number;     // X coordinate on the map
    y: number;     // Y coordinate on the map
}
